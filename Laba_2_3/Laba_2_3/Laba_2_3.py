# Ввод строки от пользователя
text = input("Введите строку: ")

# Подсчёт количества двоеточий
count = text.count(":")

# Замена двоеточий на проценты
new_text = text.replace(":", "%")

# Вывод результата
print(f"Новая строка: {new_text}")
print(f"Количество замен: {count}")

